# Игнорируем историю git (она весит много и не нужна тестам)
.git
.gitignore

# Игнорируем локальные результаты сборки (контейнер соберет всё сам заново)
target/
.mvn/wrapper/

# Игнорируем настройки IDE
.idea/
*.iml
.vscode/

# Игнорируем скрипты запуска (они нужны тебе на хосте, а не внутри контейнера)
run-tests.sh
push-tests.sh

# Игнорируем сам Dockerfile и этот файл
Dockerfile
.dockerignore

# Игнорируем локальные логи
test-output/
logs/